a# Park20 后端集成说明

## 文件清单

| 文件 | 用途 | 独立调试端口 |
|------|------|------------|
| `1-4模块.py` | 原有模块（FR-1.1 绿视率 / FR-1.2 旧场景识别 / FR-1.3 声景 / FR-1.4 GPS） | 5050 |
| `场景识别模块.py` | **新** FR-1.2 升级版（美学评分 + 情绪价值 + 人性化描述） | 5051 |
| `报告生成模块.py` | **新** FR-5.1 报告生成（随机风格 HTML + 疗愈指数计算） | 5052 |

---

## 快速集成（加入现有 1-4模块.py）

在 `1-4模块.py` 顶部 import 两个蓝图，并注册到同一个 Flask app：

```python
# 在 1-4模块.py 顶部添加：
from 场景识别模块 import scene_bp
from 报告生成模块 import report_bp

# 在 app = Flask(__name__) 之后添加：
app.register_blueprint(scene_bp)
app.register_blueprint(report_bp)
```

这样所有接口都跑在 5050 端口，前端不需要改配置。

---

## 新接口汇总

### 场景识别模块（升级版）

#### POST /api/fr12/scene_classify
与原接口路径相同，**返回格式向后兼容 + 新增字段**

请求：`multipart/form-data`，字段 `image=<图片文件>`

返回示例：
```json
{
  "status": "ok",
  "module": "FR-1.2-enhanced",
  "green_view_rate": 68.5,
  "top_label": "林荫道",
  "top_label_en": "tree-lined path",
  "top_confidence": 0.87,
  "aesthetic_score": 85,
  "emotional_value": "治愈",
  "emotion_tag": "清凉静谧",
  "healing_mood": "适合冥想与深度放松",
  "scene_description": "浓密的梧桐林荫道，阳光透过树叶洒下斑驳光影",
  "healing_comment": "漫步其中，仿佛置身天然氧吧，身心都得到了放松",
  "dimension_scores": {
    "green_experience": 70,
    "color_harmony": 88,
    "visual_richness": 75,
    "light_quality": 82
  },
  "all_results": [
    { "label_cn": "林荫道", "label_en": "tree-lined path", "emoji": "🌳", "confidence": 0.87, "emotion_tag": "清凉静谧", "healing_mood": "适合冥想与深度放松" },
    { "label_cn": "城市公园", "label_en": "urban park", "emoji": "🌇", "confidence": 0.70, ... }
  ],
  "elapsed_sec": 0.12,
  "within_5s": true
}
```

#### POST /api/fr12/aesthetic
仅返回美学评分（比完整场景识别快）

请求：同上

返回：
```json
{
  "status": "ok",
  "aesthetic_score": 85,
  "emotional_value": "治愈",
  "dimension_scores": {
    "green_experience": 70,
    "color_harmony": 88,
    "visual_richness": 75,
    "light_quality": 82
  }
}
```

---

### 报告生成模块

#### POST /api/report/generate

请求（`application/json`）：
```json
{
  "record_id": "可选，关联采集记录",
  "park_name": "世纪公园",
  "duration_sec": 1200,
  "user_image": "data:image/jpeg;base64,...（可选）",
  "modules_data": {
    "green_view": {
      "green_view_rate": 68.5,
      "aesthetic_score": 85
    },
    "scene": {
      "top_label": "林荫道",
      "scene_description": "浓密的梧桐林荫道...",
      "healing_comment": "漫步其中...",
      "emotion_tag": "清凉静谧"
    },
    "soundscape": {
      "proportions": { "鸟鸣": 0.35, "水声": 0.15, "风声": 0.12, "车声": 0.08, "人声": 0.25, "其他": 0.05 },
      "naturalness_score": 72
    },
    "gps": {
      "smoothed_points": [{"lat": 31.2206, "lon": 121.5512}, "..."],
      "total_distance_m": 850
    },
    "health": {
      "avg_hr": 78,
      "hr_start": 88,
      "hr_end": 72,
      "rmssd": 45
    }
  }
}
```

返回：
```json
{
  "status": "ok",
  "report_id": "abc123def456",
  "report_html": "<!DOCTYPE html>...",
  "healing_index": 82,
  "healing_level": "优秀",
  "report_url": "/api/report/abc123def456",
  "generated_at": "2026-06-13 14:30:00",
  "elapsed_sec": 0.08,
  "within_10s": true
}
```

#### GET /api/report/mock
无需参数，返回随机演示报告，用于前端调试。

#### GET /api/report/\<report_id\>
查询已生成的报告（JSON）。

#### GET /api/report/preview/\<report_id\>
在浏览器中直接预览报告 HTML（Content-Type: text/html）。

---

## 测试命令

```bash
# 测试场景识别（需要一张图片）
curl -X POST http://127.0.0.1:5050/api/fr12/scene_classify \
  -F "image=@/path/to/park_photo.jpg"

# 测试演示报告（不需要真实数据）
curl http://127.0.0.1:5050/api/report/mock

# 测试生成报告
curl -X POST http://127.0.0.1:5050/api/report/generate \
  -H "Content-Type: application/json" \
  -d '{
    "park_name": "世纪公园",
    "duration_sec": 1200,
    "modules_data": {
      "green_view": {"green_view_rate": 68.5, "aesthetic_score": 85},
      "scene": {"top_label": "林荫道", "scene_description": "浓密树荫道", "healing_comment": "令人放松", "emotion_tag": "清凉静谧"},
      "soundscape": {"proportions": {"鸟鸣": 0.40, "水声": 0.15, "风声": 0.15, "车声": 0.10, "人声": 0.15, "其他": 0.05}, "naturalness_score": 72},
      "gps": {"smoothed_points": [{"lat": 31.2206, "lon": 121.5512}, {"lat": 31.2210, "lon": 121.5520}], "total_distance_m": 850},
      "health": {"avg_hr": 78, "hr_start": 88, "hr_end": 72, "rmssd": 45}
    }
  }'
```

---

## 依赖安装

```bash
pip install flask flask-cors Pillow numpy
```

（与原有 1-4模块.py 依赖完全相同，无新增必须依赖）

---

## 场景识别逻辑升级说明

| 原版 | 升级版 |
|------|--------|
| 6 种场景 | 8 种场景（新增竹林小径、古建园林、阳光草坪、花海） |
| 纯颜色规则 | 色调 + 饱和度 + 亮度 + 绿视率多维条件 |
| 仅返回标签 | 标签 + 美学评分 + 情绪价值 + 人性化描述 + 疗愈评语 |
| 描述固定 | 每个场景 3 条描述文案随机选取，每次有变化 |

## 美学评分维度

```
绿视体验分 = 绿色占比×0.40 + 色彩和谐度×0.25 + 视觉丰富度×0.20 + 光线质量×0.15
```

| 维度 | 计算方式 |
|------|---------|
| 绿色占比 | 直接使用绿视率归一化值 |
| 色彩和谐度 | 高斯曲线，最优饱和度≈0.35；加权色调凝聚度 |
| 视觉丰富度 | Shannon 熵计算色调多样性，最优值≈0.55 |
| 光线质量 | 高斯曲线，最优亮度≈0.52；加光影层次奖励 |

## 报告随机化设计

- **5 套主题**：森系清新 / 水彩温柔 / 简约克制 / 手绘自然 / 日系禅意
- **4 种文案风格**：文艺风 / 治愈风 / 数据风 / 活泼风
- 每个场景 3 条描述文案 + 3 条疗愈评语随机组合
- 装饰元素位置和密度随机生成
